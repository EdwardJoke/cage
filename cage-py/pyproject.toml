[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[tool.maturin]
module-name = "cage._native"

[project]
name = "coplex-cage"
version = "0.1.0"
description = "Python bindings for the cage WASM orchestrator"
authors = [{name = "Coplex"}]
license = {text = "Apache-2.0"}
requires-python = ">=3.8"
